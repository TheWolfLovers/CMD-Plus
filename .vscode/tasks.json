{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: External Terminal with Pause",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/run_wrapper.sh",
      "args": ["${file}"],
      "console": "externalTerminal"
    }
  ]
}
